{
  "dataSources": [
    {
      "id": "WEBDISP",
      "bootstrapServers": "${kafka.default.bootstrap.servers}",
      "groupId": "${webdisp.kafka.group.id}",
      "topicName": "${webdisp.kafka.topic.name}",
      "topicPartition": 0,
      "dataClass": "${webdisp.datasource.dataClass}",
      "streamingJobClass": "${webdisp.datasource.jobClass}",
      "consumerClass": "${webdisp.datasource.consumerClass}"
    },
    {
      "id": "JARVIS",
      "bootstrapServers": "${kafka.default.bootstrap.servers}",
      "groupId": "${jarvis.kafka.group.id}",
      "topicName": "${jarvis.kafka.topic.name}",
      "topicPartition": 0,
      "dataClass": "${jarvis.datasource.dataClass}",
      "streamingJobClass": "${jarvis.datasource.jobClass}",
      "consumerClass": "${jarvis.datasource.consumerClass}"
    },
    {
      "id": "INT002",
      "bootstrapServers": "${kafka.default.bootstrap.servers}",
      "groupId": "${int002.kafka.group.id}",
      "topicName": "${int002.kafka.topic.name}",
      "topicPartition": 0,
      "dataClass": "${int002.datasource.dataClass}",
      "streamingJobClass": "${int002.datasource.jobClass}",
      "consumerClass": "${int002.datasource.consumerClass}"
    },
    {
      "id": "CONDUZIONE",
      "bootstrapServers": "${kafka.default.bootstrap.servers}",
      "groupId": "${conduzione.kafka.group.id}",
      "topicName": "${conduzione.kafka.topic.name}",
      "topicPartition": 0,
      "dataClass": "${conduzione.datasource.dataClass}",
      "streamingJobClass": "${conduzione.datasource.jobClass}",
      "consumerClass": "${conduzione.datasource.consumerClass}"
    }
  ]
}